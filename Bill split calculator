# Create a title for the program
print("Bill Split Calculator")

# Create 2 variables for the bill and tip amount
# Get the user input 
bill_amount = float(input())
tip_percentage = float(input())

# Add an option to split the bill
split_amount = int(input()) 


# Calaculate the tip amount using a formula
tip_amount = (tip_percentage / 100) * bill_amount


# Add the tip amount and bill amount
total = tip_amount + bill_amount

# Calcualte the amount per person to split the bill 
split_amount = total / split_amount

# Output the total to the user
print(f"Total (including tip): ${total}")

# Output the split amount per person for the bill 
print(f"Each person pays: ${split_amount}")
